## High-end Beauty Salon Management App (Portuguese UI) with Agenda, Inventory, Services, Loyalty, Portfolio, and Status Updates

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
